spring:

  datasource:
    url: jdbc:mysql://localhost:3306/bigboss
    username: root
    password: Mysql@2026
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true